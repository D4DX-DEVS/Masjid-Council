# Masjid Council

Monorepo.

- `MasjidCouncil-backend/` — Express/Mongo API
- `MasjidCouncil-frontend/` — React (Vite) client

Both packages keep their own `package.json`; install and run each independently.
